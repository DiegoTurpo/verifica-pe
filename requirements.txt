# Dependencias del DEMO desplegado (Streamlit Community Cloud) — mantener ligero.
# El app solo necesita leer la base cacheada y pintar el semáforo.
streamlit>=1.40
duckdb>=1.1
google-genai>=1.0
fpdf2>=2.7
python-dotenv>=1.0
