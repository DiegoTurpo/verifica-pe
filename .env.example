# Copia este archivo a .env y coloca tu API key real.
# NUNCA subas el .env real al repositorio (ya está en .gitignore).
#
# API key gratuita de Gemini: https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Opcional — modelo de Gemini (por defecto: gemini-2.0-flash-lite).
GEMINI_MODEL=
